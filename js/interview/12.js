console.log({}=={});
console.log({}==={});



// In JavaScript, objects are compared by reference, not by value.
// When you create two empty objects ({} and {}), each one is stored in a different memory location.
// Even though they have the same structure (empty), they are distinct objects.
// false
// false